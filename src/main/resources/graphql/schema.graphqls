type Query {
    products: [Product]
    product(id: Int!): Product
    categories: [Category]
    category(id: Int!): Category
    productsByCategory(categoryId: Int!): [Product]
}
type Product {
    id: Int!
    name: String!
    category: Category
}

type Category {
    id: Int!
    name: String!
}